# 数据源配置：使用哪个数据库
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/cqjtu?useUnicode=true&characterEncoding=utf-8
spring.datasource.username=root
spring.datasource.password=root123
spring.datasource.driver-class-name=com.mysql.jdbc.Driver
# jpa配置
# 控制hibernate的ddl生成策略，create为每次创建
spring.jpa.hibernate.ddl-auto=create
# 是否打印SQL
spring.jpa.show-sql=true
# 日志级别配置
logging.level.org.springframework.boot=INFO

#######dubbo#########
## 应用配置
#dubbo.application.id=bootdemo
#dubbo.application.name=bootdemo
#
## 协议配置
#dubbo.protocol.id = dubbo
#dubbo.protocol.name = dubbo
#dubbo.protocol.port = 12345
#
## 注册中心配置
#dubbo.registry.id = my-registry
#dubbo.registry.address = zookeeper://127.0.0.1:2181
#
## 服务配置
#dubbo
spring.application.name=bootdemo-dubbo
spring.dubbo.server=true
spring.dubbo.registry=zookeeper://localhost:2181
